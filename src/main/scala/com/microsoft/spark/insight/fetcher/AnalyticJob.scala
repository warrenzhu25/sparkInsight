package com.microsoft.spark.insight.fetcher

case class AnalyticJob(
                        appId: String,
                        name: String,
                        trackingUrl: String
                      )
